""" Install autofile
"""
from distutils.core import setup


setup(name="autofile",
      version="0.2.0",
      packages=["autofile",
                "autofile.system",
                "autofile.file",
                "autofile.info"])
