[project]
name = "autofile"
version = "2025.10.1"
description = "Filesystem database manager for AutoMech"
readme = "README.md"
dependencies = []
requires-python = ">= 3.11"

[dependency-groups]
test = ["pytest"]

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

# Hatchling configurations
[tool.hatch.build.targets.whell]
packages = ["src/autofile"]

# Pixi configurations
[tool.pixi.workspace]
channels = ["conda-forge", "Auto-Mech"]
platforms = ["linux-64"]
preview = ["pixi-build"]

#   - Build
[tool.pixi.package] 
name = "autofile"

[tool.pixi.package.build] 
backend = { name = "pixi-build-python", version = "*" }

[tool.pixi.package.host-dependencies] 
hatchling = "==1.26.3"

[tool.pixi.package.run-dependencies] 
numpy = ">=1.26"
pyyaml = ">=6.0.3"
autochem = ">=0.2025.0,<2.0.0"
autoio = ">=0.2025.0,<2.0.0"

#   - Environments
[tool.pixi.pypi-dependencies]
autofile = { path = ".", editable = true }

[tool.pixi.dependencies]
numpy = ">=1.26"
pyyaml = ">=6.0.3"
autochem = ">=0.2025.0,<2.0.0"
autoio = ">=0.2025.0,<2.0.0"

[tool.pixi.feature.build.dependencies]
rattler-build = ">=0.47.1"

[tool.pixi.environments]
test = ["test"]
build = ["build"]

#   - Tasks
[tool.pixi.feature.test.tasks]
test = "pytest -v src/autofile"

[tool.pixi.feature.build.tasks]
upload = "rattler-build upload anaconda -o Auto-Mech *.conda"
